<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"
        xmlns:thr="http://purl.org/syndication/thread/1.0"
        xmlns:activity="http://activitystrea.ms/spec/1.0/"
        xmlns:poco="http://portablecontacts.net/spec/1.0"
        xmlns:media="http://purl.org/syndication/atommedia"
        xmlns:ostatus="http://ostatus.org/schema/1.0"
        xmlns:mastodon="http://mastodon.social/schema/1.0">

        <id>{{ user.feedURL }} </id>
        <title>{{ user.username }}</title>
        <subtitle>{{ user.bio }}</subtitle>

        <updated>{{ date }}</updated>
        <icon>{{ user.avatar.url }}</icon>
        <logo>{{ user.avatar.url }}</logo>

        <author>
                <id>{{ user.profileURL }}</id>
                <activity:object-type>http://activitystrea.ms/schema/1.0/person</activity:object-type>
                <name>{{ user.username }}</name>
                <email>{{ user.username }}@{{ server_name }}</email>
                <summary type="html">&lt;p&gt;{{ user.bio | escape }}&lt;/p&gt;</summary>
                <link rel="avatar" type="{{ user.avatar.mime_type}}" media:width="{{ user.avatar.width }}" media:height="{{ user.avatar.height}}" href="{{ user.avatar.url }}"/>
                <link rel="header" type="{{ user.header.mime_type}}" media:width="{{ user.header.width }}" media:height="{{ user.header.height}}" href="{{ user.header.url }}"/>
                <poco:preferredUsername>{{ user.username }}</poco:preferredUsername>
                <poco:displayName>{{ user.username }}</poco:displayName>
                <poco:note>{{ user.bio }}</poco:note>
                <mastodon:scope>public</mastodon:scope>
        </author>

        <link rel="self" type="application/atom+xml" href="{{ feedURL }}"/>
        <link rel="hub" href="{{ hubURL }}"/>
        <link rel="salmon" href="{{ user.salmonURL }}"/>

        {% for s in statuses %}
        <entry>
                <id>{{ s.url }}</id>
                <published>{{ s.created }}</published>
                <updated>{{ s.created }}</updated>
                <title>{{ s.title }}</title>
                <activity:object-type>http://activitystrea.ms/schema/1.0/note</activity:object-type>
                <activity:verb>http://activitystrea.ms/schema/1.0/post</activity:verb>
                <link rel="alternate" type="application/activity+json" href="{{ s.activityURL }}"/>
                <content type="html">{{ s.content | escape }}</content>
                <link rel="mentioned" ostatus:object-type="http://activitystrea.ms/schema/1.0/collection" href="http://activityschema.org/collection/public"/>
                <mastodon:scope>public</mastodon:scope>
                <link rel="self" type="application/atom+xml" href="{{ s.atomURL }}"/>
                <ostatus:conversation ref="{{ s.ostatus_tag }}"/>
        </entry>
        {% endfor %}
</feed>
